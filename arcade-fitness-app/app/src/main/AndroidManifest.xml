<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Network permissions ──────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Required to receive CONNECTIVITY_CHANGE broadcasts on older Android versions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArcadeFitness"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ── Activities ────────────────────────────────────────────────────── -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ArcadeFitness.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".activities.WorkoutPlannerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.WorkoutTrackingActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ExerciseLibraryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ProgressTrackingActivity"
            android:exported="false" />

        <!-- ── NetworkChangeReceiver ─────────────────────────────────────────── -->
        <!--
            CONNECTIVITY_CHANGE is deprecated on Android 7+ for manifest receivers,
            but still works for apps with ACCESS_NETWORK_STATE.
            For Android 7+ we also register it programmatically in DashboardActivity.
        -->
        <receiver
            android:name=".network.NetworkChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>