<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArcadeFitness">

        <!-- ══ PHASE 1 — ONBOARDING ══ -->

        <!-- Splash: launcher entry point -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ArcadeFitness.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- Register -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- Dashboard -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ══ PHASE 2 — CORE FEATURES (add here as you build) ══ -->
        <!--
        <activity android:name=".activities.WorkoutPlannerActivity" ... />
        <activity android:name=".activities.ExerciseLibraryActivity" ... />
        <activity android:name=".activities.WorkoutTrackingActivity" ... />
        <activity android:name=".activities.ProgressTrackingActivity" ... />
        -->

        <!-- ══ PHASE 3 — TOOLS & PROFILE (add here as you build) ══ -->
        <!--
        <activity android:name=".activities.WorkoutHistoryActivity" ... />
        <activity android:name=".activities.GoalSettingActivity" ... />
        <activity android:name=".activities.BmiCalculatorActivity" ... />
        <activity android:name=".activities.UserProfileActivity" ... />
        -->

    </application>

</manifest>
